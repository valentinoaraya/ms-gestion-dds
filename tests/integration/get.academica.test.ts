import request from "supertest"
import { crearEntidadesPadre } from "../utils"

test("debería hacer una petición al endpoint y obtener los datos a partir de una Especialidad", async () => {
    const { especialidad } = await crearEntidadesPadre()



})